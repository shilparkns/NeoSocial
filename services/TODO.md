TODO:

- Move DB credentials to a config file or environment variables
- Validate email format (basic check)
- Add better error messages (e.g., “Invalid credentials” vs. “Username doesn’t exist”)
- Remove user alicia
